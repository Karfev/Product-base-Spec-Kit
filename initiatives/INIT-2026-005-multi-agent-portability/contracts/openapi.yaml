# OpenAPI 3.1.0 — SpecKit Multi-Agent Portability
# This initiative adds documentation and tooling for agent portability.
# No REST API endpoints are introduced.

openapi: "3.1.0"
info:
  title: "SpecKit Multi-Agent Portability"
  version: "0.1.0"
  description: >
    This initiative adds AGENTS.md, setup guides, compatibility matrix,
    and OpenCode symlink. No REST API endpoints are introduced.
paths: {}
